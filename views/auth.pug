extends layout

block content
  form(name="loginForm")
    input(type="text" name="login" placeholder="login")
    input(type="password" name="pwd" splaceholder="password")
    br
    button(type="submit") sign in
  script(src='/javascripts/auth.js')